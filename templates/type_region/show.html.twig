{% extends 'base.html.twig' %}

{% block title %}RegionType{% endblock %}

{% block body %}
    <div class="d-flex flex-sm-row flex-column">
        <div class="mr-auto p-2">
            <a class="btn-action btn-back" href="{{ path('type_region_index') }}"><span class="icon-Back"></span></a>
            <h1>TypeRegion</h1>
        </div>
        <div class="p-2"><a class="btn-action" href="{{ path('type_region_edit', {'id': region_type.id}) }}"><span class="icon icon-Pen"></span> Edit</a></div>
        <div class="p-2">{{ include('type_region/_delete_form.html.twig') }}</div>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_region.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_region.name }}</td>
            </tr>
            <tr>
                <th>MaxBuilding</th>
                <td>{{ type_region.maxBuilding }}</td>
            </tr>
        </tbody>
    </table>

    

    

    
{% endblock %}
